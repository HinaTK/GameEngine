目标（想达到的目标）：
	1、文档型（由于excel是关系型的，之前导出的配置都是关系型的）
	2、动态导出（只导出修改过的配置）
	3、还原配置本身的意义，配置只是配置
		让大部分配置需要计算部分（例如：概率、权重），生成配置后已经完成计算；计算不限于在excel中计算，还是导出工具中计算。


实现：
=> 双向映射
	例如id 与 name

=> 缺少数据的默认处理

=> 实现内嵌表table

=> array 在C++中数据类型处理

废弃：
=> 实现weight(在lua中可以再实现自己的模板)
	* 可以让策划自己在excel中实现累加
	* lua获取table长度最后一个作为最大概率
	* c++获取vector最后一个作为最大概率
	* erlang单独模板
	例如：通过判断键值不同，进行累加
	=IF(A5<>A4,L5,L5+F4)
	A4 A5前后键值；F4 上一累加权重值，L5下一相对权重值